"""Roblox anti-cheat bypass placeholder.

This module is intentionally minimal. Real anti-cheat bypass logic must be added
in a secure, private environment and should never be distributed externally.
"""


def apply_bypass(lua_runtime):
    """Currently a no-op. Replace with real logic when required."""
    return lua_runtime